# Tasks

## 1. 契约、依赖与配置基线

- [x] 1.1 扩展 `backend/api/openapi/velis.yaml`，定义联合文章来源、本人文章、资产、管理员文章和 Source/抓取历史端点、ETag/幂等头及新错误码，并用仓库契约校验或 YAML 解析测试验证文档有效
- [x] 1.2 为 Markdown 渲染、S3 兼容对象存储和 WebP 配置解析选择并加入最小 Go 依赖，执行 `go mod tidy`、许可证核对和现有 Go 测试验证依赖可构建
- [x] 1.3 扩展 YAML、`VELIS_*` 环境变量和示例配置，覆盖资产端点/Bucket/配额/过期时间、幂等保留期及专用 Feed 代理，并用配置优先级、非法边界和敏感值不落日志测试验证
- [x] 1.4 更新 Compose/MinIO 初始化与精确 Web Origin CORS 配置，验证私有 Bucket 可预签名上传且未授权对象不能通过 MinIO 公网端点读取

## 2. 数据模型与历史迁移

- [x] 2.1 新增版本化 up migration，创建统一文章/修订、资产/引用、幂等操作和 Source 抓取运行结构，并扩展 Source 周期与 `lock_version`；通过迁移测试验证约束、外键、部分索引和 identity sequence
- [x] 2.2 在迁移中把现有 RSS 文章无损回填为 revision 1，保留 ID、来源身份、正文和哈希，以 `discovered_at` 回填 `published_at` 并映射 hidden 状态；用 I2 前结构夹具逐字段核对升级结果
- [x] 2.3 实现受保护 down migration，只在旧模型可无损表达时还原，并用“纯历史 RSS 可降级”与“存在投稿/资产/多版本时原子拒绝”测试验证
- [x] 2.4 扩展集成测试清理器和夹具以覆盖所有 I2 表，验证测试只连接专用 `_test` 数据库且清理顺序不破坏外键

## 3. 统一文章领域与持久化

- [x] 3.1 重构文章领域模型，加入来源互斥不变量、生命周期、下架优先级、不可变修订和独立 `lock_version`，用无 DB 单元测试覆盖全部允许/拒绝的状态与权限转换
- [x] 3.2 实现用户 Markdown 规范化、`asset:<uuid>` 提取、禁用原始 HTML的渲染、白名单清洗、纯文本/摘要及内容哈希，并用恶意 HTML、外部图片、Unicode、大小和图片数量边界测试验证
- [x] 3.3 重写 PostgreSQL 文章仓储以创建/读取不可变修订、原子切换当前指针、执行乐观锁和来源互斥查询，并用集成测试覆盖并发编辑、无变化编辑和状态变化不建修订
- [x] 3.4 演进 RSS upsert，使新条目自动创建统一文章和 revision 1、内容变化创建新修订且不改变发布时间或管理员下架状态，并用仓储/服务测试覆盖插入、更新、不变和下架更新
- [x] 3.5 将匿名 latest/详情查询改为联合来源投影和 `(published_at, id)` 游标，验证 RSS/投稿混合分页、相同时间次序、编辑不顶回首页及私有状态 404

## 4. 通用幂等与文章应用用例

- [x] 4.1 实现 `idempotency_operations` 仓储和规范化命令摘要，覆盖 pending/succeeded、24 小时过期、同键同请求重放和异请求冲突，并用并发集成测试证明只提交一次
- [x] 4.2 实现本人文章创建草稿/直接发布、列表和私有详情用例，验证草稿宽松校验、发布严格校验、固定发布时间、资源归属和匿名即时可见性
- [x] 4.3 实现本人文章编辑、发布、作者下架和软删除用例，把文章变更与幂等结果置于同一事务，并验证响应丢失重试、旧 `If-Match`、管理员下架阻断和删除终态
- [x] 4.4 实现管理员文章下架/恢复用例和角色检查，验证管理员不能读取草稿、编辑/删除投稿或恢复作者下架文章，且 RSS 更新不能复活管理员下架
- [x] 4.5 扩展本地 scheduler 清理过期幂等记录，验证批量上限、重复执行和失败重试不会删除保留期内结果

## 5. 文章 HTTP 接口与装配

- [x] 5.1 实现 `Idempotency-Key`、强 `If-Match`/ETag 解析和统一内容错误映射，验证缺失/非法头、幂等键复用、文章冲突、管理员锁定和不存在的状态码/错误码
- [x] 5.2 实现 `/api/v1/me/articles` 创建/列表/详情/编辑/发布/下架/删除及无状态服务端预览处理器，使用严格 JSON 和正文上限测试验证未知字段、超限、归属和所有成功响应
- [x] 5.3 实现 `/api/v1/admin/articles/{id}/offline|restore` 并在认证开启时条件注册本人/管理员路由，验证 auth 关闭时匿名接口存在而 `/me/*`、`/admin/*` 不注册
- [x] 5.4 更新匿名文章 DTO/处理器为 `origin` 判别联合结构并保留 `/api/v1/articles` 路径，运行 HTTP 路由与 OpenAPI 契约测试验证 RSS 和用户响应

## 6. 私有图片资产

- [x] 6.1 实现资产领域和 PostgreSQL 仓储，覆盖 pending/ready/delete_pending/deleted、所有权、单文章绑定、版本引用、额度计数和匿名授权查询，并用领域/集成测试验证越权和并发确认
- [x] 6.2 实现 MinIO/S3 适配器的私有 Bucket 检查、15 分钟预签名直传、HEAD、受限签名/尺寸探测、GET/HEAD 流式读取和幂等删除，用真实 MinIO 集成测试覆盖 JPEG/PNG/WebP 及故障映射
- [x] 6.3 实现资产创建和确认应用用例，强制单文件 10 MiB、8192×8192、4000 万像素、1 GiB 用户额度和 20 个 pending，并验证同键重放不重复创建或计费
- [x] 6.4 把资产批量校验、首次文章绑定和 `article_asset_references` 写入文章修订事务，验证跨文章复用、他人资产、未确认资产、20 张/50 MiB 边界失败时不产生部分修订
- [x] 6.5 实现 `/api/v1/me/assets` 创建/确认和 `/api/v1/assets/{id}/content` GET/HEAD 处理器，验证作者草稿预览、匿名当前公开引用、下架即时拒绝、安全响应头、ETag 和不整包缓冲
- [x] 6.6 扩展 scheduler 清理 24 小时 pending、7 天未绑定 ready 和软删除文章的 delete_pending 对象，使用故障注入验证对象删除失败可重试且下架文章资产不被删除
- [x] 6.7 将 MinIO 作为局部可降级依赖装配，验证未配置/故障时资产端点返回 `ASSET_UNAVAILABLE`，`readyz`、RSS、latest、Source 管理和纯文本投稿保持可用

## 7. Source 管理、运行历史与 SSRF

- [x] 7.1 扩展 Source 领域/仓储以支持 5 分钟至 24 小时周期、默认 30 分钟、`lock_version` 和不可变 Feed URL，并用领域/集成测试覆盖周期边界、暂停/恢复和旧版本冲突
- [x] 7.2 实现 `source_fetch_runs` 仓储和游标分页，记录 scheduled/manual、运行/成功/304/失败/中止及条目统计，并验证默认 50、上限 100、敏感正文/凭据不持久化
- [x] 7.3 重构 Source 抓取编排，使运行创建、租约、文章修订、Source 完成和历史结果遵循设计的事务/fencing 边界，并用崩溃遗留、租约丢失、并发手动/定时抓取测试验证
- [x] 7.4 实现 Source 新增、列表/详情、周期 PATCH、暂停、恢复、同步手动抓取和历史应用用例，统一幂等/`If-Match` 并验证重复 URL、同键重试和运行中返回同一运行 ID
- [x] 7.5 实现 `/api/v1/admin/sources` HTTP 处理器和管理员中间件，验证普通用户拒绝、严格请求、ETag、稳定错误映射及 auth 条件装配
- [x] 7.6 改造 Feed Fetcher 默认忽略通用环境代理，并加入专用可信代理配置与脱敏启动警告；用 fake resolver/proxy 测试覆盖 IPv4/IPv6 受限地址、混合 DNS、重定向、端口、超时、5 MiB 和代理选择
- [x] 7.7 更新 `velis-admin source` 命令复用扩展后的应用服务并展示周期/版本/运行结果，运行 CLI 单元测试验证既有添加、列表、暂停、恢复、抓取入口兼容

## 8. Web 投稿与管理员界面

- [x] 8.1 更新 TypeScript API 类型和客户端以支持联合 `origin`、ETag/`If-Match`、幂等键复用、本人文章、资产和 Source API，并用 fetch mock 测试验证头、错误码和认证刷新重试不更换操作键
- [x] 8.2 更新 latest 与详情组件按 RSS Source 或站内作者展示联合来源，验证原文链接只出现在 RSS、投稿匿名阅读和现有加载/空/失败/分页状态
- [x] 8.3 新增本人文章列表、新建和编辑路由/页面，实现手动保存草稿、直接发布、立即公开编辑、下架/发布/删除、未保存离开提示和服务端预览，并用组件测试覆盖状态与权限反馈
- [x] 8.4 实现图片选择、预签名直传、确认、`asset:<uuid>` 插入和 EXIF 保留提示，验证上传进度、配额/格式失败、结果不明重试及草稿预览
- [x] 8.5 实现版本冲突 UX，确保 409 时保留本地 Markdown、允许复制并要求刷新，不自动合并或用新幂等键覆盖；用组件测试验证双标签模拟
- [x] 8.6 新增管理员 Source 页面，覆盖新增、周期修改、暂停/恢复、手动抓取和历史状态，并验证普通用户不显示入口且后端拒绝仍可正确呈现

## 9. 跨链路验证与文档收口

- [x] 9.1 增加 PostgreSQL 集成测试，覆盖历史迁移、RSS/投稿混合读取、并发编辑/状态竞争、幂等事务、权限矩阵和 Source fencing；在专用 `_test` 库执行并明确报告跳过条件
- [x] 9.2 增加 MinIO 集成与 API E2E 场景，证明预签名上传确认、公开流式读取、下架撤销、孤儿清理和 MinIO 故障局部降级；验证未运行 MinIO 时测试被明确标记而非计为通过
- [x] 9.3 增加 Web E2E 或可复现手工脚本，演示 RSS 自动发布和用户直接发布均可匿名阅读，以及编辑、作者/管理员下架、冲突和 Source 管理闭环
- [x] 9.4 更新 README、示例环境文件和运行说明，准确记录当前 I2 能力、认证/MinIO 降级矩阵、图片 EXIF、幂等窗口、受保护回滚及可信代理安全边界，并核对不把 I3+ 能力写成已实现
- [x] 9.5 运行 `gofmt`、Go 单元/架构/集成测试、`go vet`、前端 lint/typecheck/unit/build、OpenAPI/迁移验证和 `make check`，修复所有 I2 引入的问题并记录无法执行的外部依赖验证
