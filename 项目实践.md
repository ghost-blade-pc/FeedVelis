> **个人内容聚合与 Feed 阅读平台**：用户订阅新闻站点、技术博客、RSS/Atom 源，也可以上传或发布自己的 Markdown/图文文档；系统定时抓取、解析、去重并统一形成个人 Feed，支持阅读、收藏、标签、搜索和每日摘要。

它比“新闻网站”更完整，也比“RSS 阅读器”更有后端项目空间。

而且最关键的是：**你自己真的可以长期用。**

---

## 一、你给的两个项目，非常适合作为“后端骨架参考”

我看了两个仓库。

GCFeed 本质上不是单纯的“视频播放器”，而是一套完整的：

> **内容供给 → 内容存储 → Feed 分发 → 用户消费 → 互动 → 异步事件 → 监控治理**

系统。

它现在使用 Go + Gin、MySQL、Redis、RabbitMQ，已经包含 Feed 缓存、热榜、互动计数、异步 Worker、Prometheus/Grafana 和压测等工程能力。([GitHub][1])

它自己定义的完整业务闭环也是：

> 注册登录 → 发布内容 → Feed 分发 → 浏览互动 → 消息通知 → 运营监控。([GitHub][2])

另一个 `feedsystem_video_go` 更接近传统 TikTok Feed 项目，包含账户、视频、点赞、评论、关注、Feed、私信、通知；Redis 用于 Token、实体缓存、时间线和热榜，RabbitMQ 用于异步互动和 Feed 事件，并有 SSE、DLX、pprof、Docker Compose。([GitHub][3])

**你真正值得借的不是“视频”，而是它的 Feed 系统结构。**

把：

```text
视频
```

换成：

```text
Article / Document
```

很多后端设计几乎仍然成立。

---

# 二、这个项目现实中确实有对应产品，不是你凭空造的

FreshRSS 就是成熟的自托管 RSS/Atom 聚合与阅读系统，支持订阅网站、搜索、标签、OPML、WebSub，甚至针对没有 RSS 的网站提供基础 Web scraping。([GitHub][4])

Readwise Reader 则进一步把：

```text
RSS
网页文章
PDF
Newsletter
其他阅读材料
```

统一到一个阅读空间。([Readwise][5])

所以你提出的：

> 新闻 / 博客 + 自己发布的文档统一进入一个 Feed

这个产品逻辑完全成立。

甚至我认为这比“做一个纯 RSS Reader”更有意思。

---

# 三、我会怎么定义这个项目

暂时别管名字。

一句话：

> **一个基于 Go 开发的个人内容订阅与 Feed 分发平台，支持 RSS/Atom、网站内容和个人文档等多种内容源，通过定时采集、异步处理、去重和 Feed 分发，将分散的信息统一成可订阅、可阅读、可收藏和可搜索的个人信息流。**

它解决的是一个特别生活化的问题：

> **每天真正想看的内容分散在几十个网站里。**

比如你自己可能关注：

```text
Go 官方博客
Cloudflare Blog
OpenAI Blog
GitHub Blog

某些技术作者博客

F1 新闻
汽车 / 科技新闻

学校 / 招聘信息

自己的 Markdown 文档
```

现在你的方式是：

```text
浏览器收藏夹
      ↓
一个一个打开
      ↓
看看今天更新没有
```

系统做的事情就是：

```text
                Go Blog
                   │
              Cloudflare
                   │
                  F1
                   │
               博客作者
                   │
              我的文档
                   │
                   ▼
            Content Platform
                   │
           定时获取 / 发布
                   │
                 解析
                   │
                 去重
                   │
                 分类
                   │
                   ▼
                 Feed
                   │
       ┌───────────┼───────────┐
       ▼           ▼           ▼
      Today      Following    Saved
```

这个业务故事我觉得非常自然。

---

# 四、这里有一个很重要的变化：内容有两种来源

这会让项目比普通 RSS Reader 更丰满。

### 外部内容

例如：

```text
RSS / Atom
    ↓
Fetcher
    ↓
Parser
    ↓
Article
```

外部来源包括新闻、Blog、个人网站等。

### 内部内容

你自己可以：

```text
Markdown
图片
文章
项目文档
```

直接发布：

```text
Editor / Upload
      ↓
Article
```

最后两者进入相同的内容模型：

```text
                   Content Source

              ┌─────────┴─────────┐
              ▼                   ▼
          External              Internal
       RSS / Website          User Published
              │                   │
              └─────────┬─────────┘
                        ▼
                     Article
                        │
                        ▼
                       Feed
```

这其实很好。

因为你既有：

> **内容聚合系统**

又有：

> **轻量内容发布系统**

---

# 五、用户是谁也特别好解释

第一类就是你自己：

> 我希望把每天关注的技术博客、新闻、行业资讯和自己的文档集中到一个地方，不需要每天打开多个网站检查更新。

第二类是开发者、学生、研究人员、资讯重度用户：

> 有固定的信息源，希望建立自己的阅读 Feed，而不是完全依赖算法推荐。

第三类可以是轻量内容创作者：

> 自己发布文章，同时订阅其他来源。

所以你完全不需要编：

> “某企业日活百万用户……”

面试时可以很自然地说：

> **这是我自己有使用需求的项目。我平时会持续关注技术博客、行业资讯和一些固定站点，但信息来源比较分散，所以做了一个自托管内容聚合系统，把 RSS、网页内容和自己的文档统一形成 Feed。**

非常合理。

---

# 六、这个项目相比 GCFeed，最大的变化是“内容供给侧”

GCFeed：

```text
用户上传视频
       ↓
      Video
       ↓
      Feed
```

你的项目：

```text
                   Scheduler
                       │
                       ▼
RSS / Blog ───────→ Fetch Task
                       │
                       ▼
                     Parse
                       │
                       ▼
                     Article
                       │
                       │
User Publish ──────────┘
                       ↓
                      Feed
```

这多出来的一条：

> **定时任务 + 内容采集 Pipeline**

其实特别适合 Go。

---

# 七、这会自然产生一个很完整的异步任务系统

比如用户订阅：

```text
https://example.com/feed.xml
```

系统保存：

```text
Source

URL
FetchInterval = 30 min
LastFetchedAt
NextFetchedAt
ETag
LastModified
Status
```

Scheduler：

```text
每分钟
  ↓
查询 next_fetch_at <= now
  ↓
生成 Fetch Task
  ↓
RabbitMQ
  ↓
Fetcher Worker
```

Worker：

```text
GET RSS
   ↓
HTTP 200?
   ↓
Parse
   ↓
Article Dedup
   ↓
INSERT MySQL
   ↓
更新 Feed
```

如果：

```text
304 Not Modified
```

直接跳过。

如果：

```text
timeout / 500
```

进入 Retry。

于是：

```text
RabbitMQ
Worker
Retry
DLQ
定时任务
并发控制
HTTP Client
context
timeout
```

全部非常自然地出现。

这比之前为了项目专门设计一个 Task Platform 合理多了。

---

# 八、数据库也比 Velis 更适合练 MySQL

最终大概会有：

```text
users

sources
subscriptions

articles
article_contents
article_tags

user_articles
    read
    starred
    read_later

user_tags

fetch_tasks
fetch_logs

daily_digests
```

马上就会产生：

```text
User N:M Source

Source 1:N Article

Article N:M Tag

User N:M Article
```

这已经比：

```text
users
workspaces
tasks
```

丰富很多。

---

# 九、Feed 本身就能很好地练 MySQL

最普通 Feed：

```sql
ORDER BY published_at DESC, id DESC
```

然后做：

> **游标分页。**

不要：

```text
page=10000
```

而是：

```text
published_at < last_time

或者：

(published_at, id) < (?, ?)
```

GCFeed 本身就在使用 `published_at + id` 这样的 Timeline 排序和游标分页设计，而热门 Feed 使用 Redis 聚合热度。([GitHub][6])

你完全可以借鉴。

例如：

```text
GET /feed
    ↓
cursor
    ↓
MySQL
    ↓
Article IDs
    ↓
Redis MGET Article Cards
    ↓
返回 Feed
```

这是非常标准的后端场景。

---

# 十、Redis 也会非常自然

我会给它三个主要职责。

### Article Cache

热门文章：

```text
article:{id}
```

使用 Cache Aside：

```text
Redis
  ↓ miss
MySQL
  ↓
Redis
```

你就会真实碰到：

> 缓存穿透、缓存击穿、TTL、一致性。

### Feed Cache

比如：

```text
feed:user:{uid}
```

或者：

```text
feed:latest
```

保存一定窗口内的最新内容 ID。

### 热榜

如果以后支持：

```text
阅读
收藏
点赞
```

就可以：

```text
Redis ZSET
```

维护：

```text
hot:daily
```

例如：

```text
score =
views * 1
+ stars * 5
+ likes * 3
```

于是 Feed 能有：

```text
Latest
Following
Hot
```

跟 GCFeed 的业务思想非常接近。

---

# 十一、RabbitMQ 在这里甚至比视频 Feed 更容易解释

异步任务至少存在：

```text
Fetch RSS
Parse Content
Fetch Cover
Generate Digest
AI Summary
Notification
```

比如：

```text
Scheduler
   ↓
fetch.exchange
   ↓
Fetcher Worker
```

解析完成：

```text
Article Created
      ↓
article.exchange
      │
      ├── Index Worker
      ├── AI Summary Worker
      └── Notification Worker
```

你马上就能实践：

```text
Topic Exchange
ACK
Retry
DLX
幂等
重复消费
消费者并发
消息积压
```

这和 `feedsystem_video_go` 中使用 RabbitMQ 处理点赞、评论、关注、热度和 Feed 事件的思想是相通的。([GitHub][3])

但你的业务不同。

---

# 十二、“每天获取最新内容”可以成为非常漂亮的核心功能

例如用户设置：

```text
Go Blog              每 30 min
Cloudflare Blog      每 1 h
F1 News              每 15 min
个人 Blog            每 2 h
```

系统不需要做到秒级。

对于你一个月项目：

> **分钟级调度已经完全够了。**

Scheduler 可以自己用 Go 写：

```text
ticker
 ↓
扫描 sources
 ↓
claim due source
 ↓
enqueue
```

后期才考虑更复杂调度。

这里还能练：

```text
SELECT ... FOR UPDATE SKIP LOCKED
```

例如多个 Scheduler：

```text
同时扫描需要抓取的 Source
```

不能重复认领。

这就是很好的 MySQL 并发场景。

---

# 十三、内容去重也是一个很好的工程问题

RSS 来源经常重复返回最近几十篇文章。

不能每抓一次都：

```text
INSERT
INSERT
INSERT
```

可以使用：

```text
source_id
+
external_guid
```

唯一约束。

如果没有 GUID：

```text
canonical_url hash
```

例如：

```text
UNIQUE(source_id, external_id)
```

或者：

```text
UNIQUE(url_hash)
```

于是 RabbitMQ 重复消费也不怕。

这就是：

> **业务级幂等。**

非常适合面试。

---

# 十四、这里还天然存在 HTTP 缓存知识

RSS 抓取非常适合实践：

```http
ETag
If-None-Match

Last-Modified
If-Modified-Since
```

如果没有变化：

```http
304 Not Modified
```

于是你不是每半小时：

> 整个 XML 全下载一遍。

这个点虽然不是什么“大厂中间件”，但特别像真正写后端服务会碰到的问题。

面试的时候也很容易证明：

> 你不是只会 CRUD。

---

# 十五、“自己的文档”怎么融进来？

我反而建议不要再搞 Velis 那套 Git Gateway。

简单很多：

```text
Create Article
Edit Article
Publish Article
Archive Article
```

正文：

```text
Markdown
```

MySQL 可以保存正文；图片放：

```text
MinIO / S3
```

但是一个月内甚至：

> 本地对象存储 + MinIO

即可。

这样你还能顺便练：

```text
文件上传
对象存储
预签名 URL
```

但不需要 Git。

---

# 十六、AI 可以加，但是只做一小块

这点刚好特别适合你现在的情况。

不要再：

> Agent-first。

AI 在这里应该只是一个功能。

比如最有价值的：

## 每日摘要

每天晚上：

```text
今天新文章 73 篇
      ↓
根据：
我的订阅
我的标签
阅读历史
      ↓
挑选 10～20 篇
      ↓
LLM
      ↓
Daily Digest
```

输出：

```text
今天值得关注

Go
- Go 1.xx 新特性……
- Uber 发布新的 Go 工具……

AI
- OpenAI ...
- Anthropic ...

F1
- ...
```

这非常生活化。

---

# 十七、甚至可以做“AI 摘要”，但不要一上来做

例如 Feed Card：

```text
标题
来源
发布时间

AI Summary
3 句话

Tags
```

但是：

> **原始 Feed 系统必须在没有 AI 的情况下完整可用。**

AI 只是：

```text
Article
   ↓
RabbitMQ
   ↓
Summary Worker
   ↓
LLM
   ↓
article_summary
```

这么简单。

这就让你的 Agent/MCP 经历不会消失，但新项目仍然是：

> **Go 后端项目。**

---

# 十八、我甚至觉得这个项目比 AI PR Review 更适合你

现在重新打一次分：

| 项目               |       生活化 |      后端训练 |     一个月可控 |    自己长期使用 |      面试展开 |
| ---------------- | --------: | --------: | --------: | --------: | --------: |
| AI PR Review     |       ★★★ |     ★★★★☆ |      ★★★★ |      ★★★★ |     ★★★★☆ |
| Webhook Platform |        ★★ |     ★★★★★ |      ★★★★ |         ★ |     ★★★★★ |
| Agent Runtime    |         ★ |     ★★★★★ |        ★★ |        ★★ |     ★★★★★ |
| Velis            |      ★★★★ |       ★★★ |        ★★ |     ★★★★★ |      ★★★★ |
| **Content Feed** | **★★★★★** | **★★★★☆** | **★★★★★** | **★★★★★** | **★★★★☆** |

综合下来：

> **Content Feed 可能是现在最适合你的。**

因为它没有任何一项极端突出，但几乎没有明显短板。

---

# 十九、1.0 我会严格限制成这些模块

| 模块            | 1.0                                 |
| ------------- | ----------------------------------- |
| User          | 注册、登录、Token                         |
| Source        | 添加 RSS/Atom、删除、启停、分类                |
| Scheduler     | 定时发现待抓取 Source                      |
| Fetcher       | HTTP 拉取、超时、ETag、Retry               |
| Parser        | RSS/Atom 解析                         |
| Article       | 去重、保存、详情                            |
| Feed          | Latest / Following、游标分页             |
| User Action   | Read / Star / Read Later            |
| Search        | 标题、正文简单检索                           |
| Publish       | Markdown 发布自己的文章                    |
| Redis         | Article Cache、Feed Cache、限流         |
| RabbitMQ      | Fetch / Article Event / Retry / DLX |
| Observability | Metrics、pprof、日志                    |
| Deploy        | Docker Compose                      |
| AI            | **可选：文章摘要 / Daily Digest**          |

这已经非常够用了。

---

# 二十、明确一个月内不做

只留这一份“砍功能清单”：

* 不做推荐算法；
* 不做个性化机器学习；
* 不做爬遍互联网；
* RSS/Atom 优先，普通网页抓取最多支持少量规则；
* 不做 Elasticsearch 集群；
* 不做向量数据库；
* 不做 RAG；
* 不做 Agent；
* 不做多人社交；
* 不做评论区；
* 不做移动 App；
* 不做复杂 CMS；
* 不做 Kafka，RabbitMQ 足够；
* 不做微服务，**API + Worker 两个进程即可**；
* 不做 K8s。

这样一个月完全有可能完成。

---

# 二十一、开发流程也非常适合一个月

### Week 1：能真正刷 Feed

```text
Go + Gin
MySQL
用户

Source CRUD
RSS Parser
定时抓取

Article
Feed

游标分页
```

第一周结束：

> 添加几个你真的看的技术 Blog → 系统自动拉取 → 首页可以刷。

**这时候产品已经成立。**

### Week 2：异步化

加入：

```text
RabbitMQ
Worker

Scheduler → Queue → Fetcher

Retry
DLX
幂等
超时
```

再加入：

```text
Read
Star
Tag
```

### Week 3：性能和缓存

加入：

```text
Redis

Article Cache
Feed Cache
Rate Limit

ETag
HTTP Cache

Batch Query
SQL Index
```

然后压测 Feed：

```text
100 QPS
500 QPS
1000 QPS
```

观察 P95。

### Week 4：工程质量 + AI

加入：

```text
Prometheus
Grafana
pprof

Docker Compose
CI

unit test
integration test
```

有余力再：

```text
AI Summary
Daily Digest
```

然后部署到你的云服务器。

---

# 二十二、这个项目在简历上也很好写

最终不要写：

> **RSS 阅读器。**

太轻了。

可以写成：

> **Go 内容聚合与 Feed 分发平台**

项目描述可以围绕：

> 基于 Go 构建支持 RSS/Atom 与个人文档发布的内容聚合系统，通过定时调度与 RabbitMQ Worker 完成多源内容异步采集、解析和幂等入库；基于 MySQL 游标分页与 Redis 多级缓存实现个人 Feed 分发，并提供收藏、搜索、每日摘要及完整可观测能力。

这就已经很后端了。

再往下面写：

```text
Scheduler
RabbitMQ
Redis
MySQL
Feed
Idempotency
HTTP Cache
Retry/DLX
Prometheus
```

面试官有很多地方可以问。

而且你还能直接说：

> “这个项目现在就部署在我的服务器上，我每天会用它订阅 Go Blog、AI 技术博客和一些新闻源。”

**这一句话对个人项目的可信度加成，我认为比“设计支持百万 DAU”强多了。**

所以这次我会明确表态：**相比前面讨论过的几个方向，你现在提出的“新闻/博客/个人文档 Content Feed”更贴合你一个月补 Go 后端项目实践的目标。** GCFeed 很适合借它的 Feed、缓存、RabbitMQ、压测和可观测思路，但内容供给侧改成“定时采集 + 自有发布”，项目就会变成你自己的东西，而不是换皮 TikTok。([GitHub][1])

[1]: https://github.com/LeoninCS/GCFeed?utm_source=chatgpt.com "GitHub - LeoninCS/GCFeed: GoClubFeed，更为全面完整的视频Feed流系统项目。 · GitHub"
[2]: https://github.com/LeoninCS/GCFeed/blob/main/docs/product.md "GCFeed/docs/product.md at main · LeoninCS/GCFeed · GitHub"
[3]: https://github.com/LeoninCS/feedsystem_video_go?utm_source=chatgpt.com "GitHub - LeoninCS/feedsystem_video_go: 一款基于Go语言的视频feed流系统（仿tiktok项目）。 · GitHub"
[4]: https://github.com/freshrss/freshrss?utm_source=chatgpt.com "GitHub - FreshRSS/FreshRSS: A free, self-hostable news aggregator… · GitHub"
[5]: https://readwise.io/read?utm_source=chatgpt.com "Readwise Reader | The first read-it-later app built for power readers."
[6]: https://github.com/LeoninCS/GCFeed/blob/main/docs/architecture.md "GCFeed/docs/architecture.md at main · LeoninCS/GCFeed · GitHub"


可以，而且我觉得这反而会让这个项目更完整，但前提是：**Agent 必须是 Feed 系统上的“增强层”，不能反过来把整个项目变成 Agent 平台。**

最合适的定位不是“做一个万能 Agent”，而是：

> **在个人内容聚合平台中加入一个 Content Agent，帮助用户基于已订阅、已抓取和已收藏的内容进行查找、整理、推荐和总结。**

这样整个项目的主线仍然非常清楚：

```text
内容采集与 Feed 分发        ← Go 后端主体
        ↓
统一内容库
        ↓
搜索 / 用户行为
        ↓
Content Agent              ← AI 增强能力
```

这比单独再做一个 Agent Task Platform 自然得多。

---

## 一、Agent 在这个项目里到底解决什么问题？

假设你一天订阅了：

* Go 官方博客；
* Cloudflare；
* OpenAI；
* Anthropic；
* 一些个人技术博客；
* F1 新闻；
* 招聘资讯；
* 自己发布的 Markdown；
* 收藏的文章。

一天可能产生几十甚至上百篇内容。

传统 Feed 解决的是：

> “今天有哪些新内容？”

但当信息越来越多以后，会出现第二层问题：

> “这么多内容，我到底应该看什么？”

这时候 Agent 就有明确价值。

它不是负责产生内容，而是：

> **帮助用户消费已有内容。**

---

# 二、我建议 Agent 1.0 只做四件事

不要一开始做万能 Agent。

### 1. 内容查找

用户可以直接问：

> 最近有没有讲 Go GC 的文章？

> 我上周收藏过一篇关于 Kafka 幂等的文章，帮我找出来。

> 最近 Cloudflare 有没有发布数据库相关内容？

Agent：

```text
用户问题
   ↓
识别查询意图
   ↓
search_articles
   ↓
获取候选文章
   ↓
read_article
   ↓
组织回答
```

这其实特别适合做 Tool Calling。

例如只需要几个工具：

```text
search_articles(query, filters)

get_article(id)

list_sources()

get_saved_articles()
```

甚至暂时不需要复杂 RAG。

---

# 三、第二个能力：辅助推荐

这个我觉得很适合你的产品。

传统首页：

```text
Latest
Following
Hot
```

Agent 可以支持：

> “我今天只有 20 分钟，推荐 5 篇最值得看的技术文章。”

或者：

> “今天别给我推荐 AI 新闻，多推荐 Go 和后端。”

或者：

> “最近我在准备 Redis 面试，帮我从这周的内容里挑一些值得看的。”

Agent 可以综合：

```text
用户明确需求
+
订阅源
+
文章标签
+
收藏记录
+
阅读历史
+
发布时间
```

得到结果。

注意这里我不建议你第一版直接搞：

```text
Collaborative Filtering
深度推荐模型
Embedding Recommendation
```

完全没必要。

甚至第一版可以是：

```text
数据库候选召回
       ↓
规则打分
       ↓
LLM rerank / explanation
```

例如：

```text
score =
时间新鲜度
+ 用户感兴趣 tag
+ source 权重
+ 未读
+ 收藏相似主题
```

然后让 LLM 对前 20 篇重新排序并解释：

> “为什么推荐这 5 篇？”

这既可控，又真实。

---

# 四、第三个能力：内容问答

假设你已经有：

```text
1000 篇 Article
```

用户问：

> 最近几篇关于 Go 1.26 的文章主要在讨论什么？

Agent：

```text
Search
 ↓
找到 8 篇
 ↓
读取内容摘要
 ↓
综合回答
```

或者：

> 我之前收藏的 Kafka 文章里，关于 Exactly Once 是怎么说的？

这个能力会让你的平台开始真正区别于普通 RSS Reader。

---

# 五、第四个能力：每日内容助手

这个其实是我最推荐的 AI 功能。

比如每天晚上自动生成：

> **Your Daily Brief**

内容：

```text
今天抓取 63 篇文章

值得优先阅读：6 篇

Go
1. ...
2. ...

AI
1. ...
2. ...

F1
1. ...

你昨天收藏的 Redis 主题今天有 2 篇相关更新。
```

而且用户还能继续问：

> “第二篇展开讲讲。”

这时候 Daily Digest 就从一个静态摘要变成一个 Agent 入口。

产品体验会很完整。

---

# 六、这样一来，项目业务闭环就非常漂亮了

原本是：

```text
订阅
 ↓
采集
 ↓
Feed
 ↓
阅读
```

加入 Agent 后：

```text
                  External Source
 RSS / Blog / News / Website
                      │
                      │ Fetch
                      ▼
                    Article
                      │
              ┌───────┴────────┐
              │                │
              ▼                ▼
             Feed          Search Index
              │                │
              ▼                │
          User Behavior        │
       read / star / save      │
              │                │
              └───────┬────────┘
                      ▼
                 Content Agent
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
       查找           推荐          总结
        │             │             │
        └─────────────┴─────────────┘
                      ↓
                    用户
```

这就已经很像一个真正可以使用的产品。

---

# 七、而且 Agent 可以很好地增加后端技术含量

最重要的是：

**不要把 Agent 请求直接运行在 HTTP Handler 里面。**

例如：

```text
POST /agent/chat
       ↓
创建 Agent Task
       ↓
RabbitMQ
       ↓
Agent Worker
       ↓
LLM / Tools
       ↓
task_events
       ↓
SSE
       ↓
浏览器
```

这样你之前对 Agent Task Platform 感兴趣的那一小部分，也可以合理地融进来。

但只做轻量版本。

---

# 八、你的 RabbitMQ 就能同时承担两类任务

这是一个很好的架构点。

```text
RabbitMQ

├── content.fetch
│      ↓
│   Fetch Worker
│
├── content.process
│      ↓
│   Content Worker
│
└── agent.task
       ↓
    Agent Worker
```

以后还可以：

```text
content.summary
daily.digest
```

这就真正形成了：

> **统一异步任务体系。**

面试时 RabbitMQ 就非常好讲。

---

# 九、Agent 需要哪些数据？

我建议核心数据还是 MySQL。

例如：

```text
articles
article_contents

sources

user_articles
  read
  starred
  read_later

article_tags

agent_sessions
agent_messages

agent_tasks
agent_task_events
```

Agent 不拥有自己的另一套知识。

它只是消费现有业务数据：

```text
Article
Source
Tag
User Behavior
```

这是很重要的设计。

不要搞成：

```text
Feed 有一套数据

Vector DB 又复制一整套文章
```

第一版完全没必要。

---

# 十、搜索第一版甚至不一定需要向量数据库

我强烈建议：

### V1

先用：

```text
MySQL FULLTEXT
```

或者：

```text
Bleve
```

甚至内容量很小时：

```text
LIKE / simple inverted index
```

都可以。

Agent 调：

```text
search_articles("Go GC")
```

即可。

如果后面你真的发现：

> 用户问“内存回收”时，文章里只出现“garbage collection”，关键词搜索不好用。

这时候才有真实理由引入：

```text
Embedding
Vector Search
Hybrid Search
```

---

# 十一、如果一个月能做到后期，再加 Hybrid Search

最终可以：

```text
Query
  │
  ├── Keyword Search
  │
  └── Vector Search
         │
         ↓
      Merge / Rerank
         ↓
      Candidate Articles
```

这样就是真正的：

> **Keyword + Semantic Retrieval**

但我建议明确放到：

> 1.1 / Bonus。

不要让向量库拖死 1.0。

---

# 十二、Agent 推荐也不要直接“让 LLM 从所有文章里选”

这个设计很关键。

不要：

```text
1000 篇文章全部塞给 LLM
```

正确方式：

```text
候选召回
  ↓
1000
  ↓
SQL / Search / Rule
  ↓
20
  ↓
LLM rerank
  ↓
5
```

这非常接近真实推荐/搜索系统的：

```text
Recall
 ↓
Ranking
```

哪怕你的算法很简单，架构思想是正确的。

而且特别适合面试解释：

> LLM 不承担数据库搜索职责，它只处理已经筛选好的小候选集，从而控制延迟和 token 成本。

这句话很不错。

---

# 十三、推荐系统其实可以先不依赖 AI

例如基础推荐：

```text
用户兴趣 Tag：
Go        0.9
Backend   0.8
AI        0.5
F1        0.6
```

Article：

```text
Go
GC
Runtime
```

算一个简单：

```text
interest_score
```

再加：

```text
freshness_score

source_score

read_penalty
```

得到：

```text
ranking_score
```

然后 Agent 的职责只是：

> **理解自然语言偏好 + rerank + 解释。**

比如用户说：

> “今天只想看 Go，不想看 AI。”

传统系统很难即时改变。

Agent 可以把它转换为：

```text
include_tags = [Go]

exclude_tags = [AI]

limit = 5
```

这就是一个很合理的 Agent 使用方式。

---

# 十四、这样你也终于有了一个真正好用的 Tool Calling 场景

你现有 MCP/Agent 经历可以自然迁移。

Agent Tool 可以只有：

```text
search_articles
get_article
list_latest_articles
list_saved_articles
get_user_interests
recommend_articles
```

例如：

用户：

> “帮我找一下这周关于 Redis 缓存一致性的文章，然后挑两篇最值得我面试前看的。”

Agent：

```text
① search_articles(
      query="Redis cache consistency",
      since=7d
   )

② 得到 9 篇

③ get_article / metadata

④ recommend_articles(
      candidate_ids=[...],
      goal="backend interview"
   )

⑤ 回答
```

这个 Agent 是：

> **有工具、有业务数据、有具体目标的 Agent。**

比单纯：

```text
ChatGPT Clone
```

强很多。

---

# 十五、甚至 MCP 可以成为一个可选扩展

但不要把 MCP 作为核心。

你可以设计：

```text
Content Agent
     │
     ├── Internal Tools
     │
     │    search_articles
     │    recommend
     │
     └── MCP Client  ← Bonus
           │
           └── External Tools
```

比如以后：

> 搜索 GitHub。

> 获取天气。

> 查询 F1 信息。

但这是：

> 2.0。

不要一个月里再搞通用 MCP Platform。

---

# 十六、最后这个项目会形成四层非常清楚的能力

我很喜欢这一点。

### 第一层：内容采集

```text
Scheduler
HTTP Client
RabbitMQ
Retry
DLX
Idempotency
```

### 第二层：内容存储与 Feed

```text
MySQL
Redis
Cursor Pagination
Cache
Search
```

### 第三层：用户行为

```text
Read
Star
Save
Tag
Preference
```

### 第四层：AI

```text
Agent
Tool Calling
Search
Recommendation
Summary
SSE
```

所以整个项目不会变成 AI 项目。

AI 只占：

> **最后 20～25%。**

这正是我希望你现在达到的比例。

---

# 十七、如果按一个月，我会重新规划为

### Week 1：核心 Feed

必须跑通：

```text
User
Source
RSS
Article
Feed
Read/Star
```

部署后自己开始用。

---

### Week 2：异步系统

实现：

```text
Scheduler

RabbitMQ

Fetch Worker

Retry
DLX

Dedup

Redis Cache
```

这周主要补后端。

---

### Week 3：内容能力

实现：

```text
Search

Tag

Read Later

Markdown Publish

Daily Digest 基础版
```

并开始：

```text
Metrics
pprof
压测
```

---

### Week 4：Agent

最后才：

```text
Agent Chat

Tool Calling

search_articles

get_article

recommend_articles

SSE

AI Summary
```

如果 Week 4 做不完：

> Feed 系统本身已经是一个完整 Go 后端项目。

这是这个设计最安全的地方。

---

# 十八、我认为最终产品定位可以再升级一下

不要叫：

> 新闻聚合网站。

也不要叫：

> RSS Reader + AI。

可以定义成：

> **Personal Content Hub**

或者中文：

> **个人内容聚合与智能阅读平台。**

一句话描述：

> **统一聚合技术博客、新闻、RSS 和个人文档，通过异步采集与 Feed 分发构建个人内容流，并提供基于内容库和阅读偏好的智能搜索、推荐与每日摘要。**

这个定位已经足够完整了。

---

# 十九、作为求职项目，我现在会更推荐这个版本

因为它同时给你三个面试入口。

普通 Go 面试官问：

> 项目做了什么？

你讲：

```text
Scheduler
RabbitMQ
Redis
MySQL
Feed
Cache
Retry
Idempotency
```

偏高并发面试官：

```text
Feed Pagination
Cache
Hot Data
Worker Pool
MQ Backlog
Rate Limit
pprof
```

AI 面试官：

```text
Agent
Tool Calling
Retrieval
Recommendation
LLM rerank
SSE
```

三个方向都能接。

而且面试官如果完全不关心 Agent：

> **把 AI 模块删掉，整个项目依然成立。**

我认为这一点特别重要。

这说明它首先是一个：

> **Go 后端项目。**

然后才是 AI-enhanced。

所以，**加 Agent 可以，而且我支持加；但要坚持“Feed 是产品主体，Agent 是智能交互层”这个边界。** 这样既不会重复你之前 Agent 项目过重的问题，又能自然把你已有 MCP/Agent 经历接到一个真正生活化的 Go 后端项目上。
